import type { Config } from "tailwindcss";

import baseConfig from "@workspace/tailwind-config/web";

export default {
  content: [...baseConfig.content, "../../libs/web-ui/**/*.{ts,tsx}"],
  presets: [baseConfig],
} satisfies Config;
